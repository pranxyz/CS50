#include <bits/stdc++.h>
#include<string.h>
using namespace std;
extern void convert(string hexa);

int main(){
   string hexa = "123B";
   cout << "\nEquivalent Binary value is : ";
      convert(hexa);
   return 0;
}