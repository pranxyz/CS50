#include <iostream>
#include <string>
using namespace std;
// class store chain declaration
class StoreChain
  private:
    vector<store> store_locations; //a vector to store locations
    string name; //name will be of type string
  public:
  StoreChain()
  vector 
   vector<store> get_store_locations(); //accessor function to access the store_locations vector.
    string get_name(); //accessor function to access the name
    void add_store(store store_area); //a function to add a store. The store area will be an object of the class store.
};